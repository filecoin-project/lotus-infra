apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: fullnode
spec:
  install:
    timeout: "90m"
  upgrade:
    # XXX: This is temporary so I can see if I can get this to upgrade
    timeout: "12h"
  chart:
    spec:
      version: 0.4.2
