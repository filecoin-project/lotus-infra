apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: fullnode
spec:
  install:
    timeout: "90m"
  upgrade:
    # Chain imports are reportedly taking over 8 hours
    timeout: "9h"
