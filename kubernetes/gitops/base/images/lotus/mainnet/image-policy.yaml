apiVersion: image.toolkit.fluxcd.io/v1alpha1
kind: ImagePolicy
metadata:
  name: lotus-latest-release
spec:
  policy:
    semver:
      # # Temporarily disable this since there's a stable 1.23.0 release and
      # # we want to deploy 1.22.0 instead
      # range: '~1.x'
      range: '1.22.0'
  imageRepositoryRef:
    name: filecoin-lotus-all-in-one
