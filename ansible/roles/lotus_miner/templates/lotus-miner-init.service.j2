[Unit]
Description=Lotus Miner Init
After=network.target
ConditionPathExists=!{{ lotus_miner_path }}/datastore

[Service]
User={{ lotus_user }}
Group={{ lotus_user }}
ExecStart=/usr/local/bin/lotus-storage-miner init --genesis-miner="{{ lotus_miner_genesis_miner }}" --actor="{{ lotus_miner_addr }}" --pre-sealed-sectors="{{ lotus_miner_presealed_sectors_path }}" --nosync="{{ lotus_miner_nosync }}" --sector-size="{{ lotus_miner_sector_size }}"
Environment=LOTUS_PATH="{{ lotus_path }}"
Environment=LOTUS_STORAGE_PATH="{{ lotus_miner_path }}"
Environment=GOLOG_FILE="{{ lotus_miner_golog_file }}"
Environment=GOLOG_LOG_FMT="{{ lotus_miner_golog_log_fmt }}"
{% for item in lotus_miner_env | dict2items -%}
Environment={{ item.key }}="{{ item.value }}"
{% endfor %}

[Install]
WantedBy=multi-user.target
